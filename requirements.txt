# mlcad — unified environment (recommended)
#
#   conda env create -f environment.yml
#   conda activate mlcad
#
# Or: bash scripts/setup_env.sh
#
# This single env includes:
#   - pythonocc-core (OpenCASCADE STEP/BRep parsing)
#   - torch + torch-geometric (GNN training and inference)
#   - h5py, pyyaml, numpy, scikit-learn, tqdm
#
# Verify after install:
#   python -c "import torch; from OCC.Core.STEPControl import STEPControl_Reader; import torch_geometric; print('ok')"
#
# =============================================================================
# Legacy pip-only path (H5 training/inference only — no STEP parsing)
# =============================================================================
#    pip install torch torch_geometric h5py pyyaml numpy tqdm scikit-learn
#
# STEP ingest and param enrichment require pythonocc-core (conda-forge only).
