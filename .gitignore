# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venv_pyg/
venv/

# Training artifacts
runs/
*.pt
*.ckpt

# Data — do NOT commit the 1.5GB dataset
*.h5
*.zip
*.step
*.stp
data/
MFCAD_dataset/
MFCAD++_dataset/

# NIST CTC-01 transfer-test outputs (authoritative jsonl + intermediates; do NOT commit)
nist_ctc_01_predictions.jsonl
nist_ctc_01_stage_a.npz
# (nist_ctc_01_annotated.step and nist_ctc_01.step already covered by *.step)

# OS / editor
.DS_Store
.idea/
.vscode/

# Secrets / local env
.env
.env.*
!.env.example

# Local Fusion tool-library exports (ingest once into Supabase; do not commit)
local_tool_libraries/
# Pipeline outputs (Phase 4)
pipeline_out/
*.parquet

# Recoverable archived probe scripts (cleanup/safe-artifacts)
archive/

# Legacy GNN/LLM experiment run artifacts (regenerable via final_table.py/compare_runs.py)
# NOTE: baseline_results.txt is a curated input and is intentionally NOT ignored
*_run.jsonl
*_run.errors.jsonl
*_run.cm.csv
*_run.report.txt
bounded_stripped.*
bounded_unstripped.*
llm_feat_fixed.*
llm_leak_demo.*
llm_raw_fixed.*
train_11dim.log

# Generated feature-graph viewers (regenerable via feature_graph_viewer/build.py)
*_graph_viewer.html

# Generated CAM plan output (regenerable via seed_and_plan.py / generate_operation_plan.py)
/plan.json

# Diagnostic run artifacts (regenerable via diag/ scripts)
diag/out_*.txt
diag/*.npz
diag/step_candidates.json
diag/dup_edge_cases.json

# Heuristic baseline run logs
heuristic_baselines/*.log

# Old 25-class dataset explorer cache (regenerable)
dataset_explorer/cache_old25/

# GNN training run artifacts (Path 1 / legacy track; regenerable via training)
runs_cloud/
runs_cloud_latest/
runs_cloud_25class_backup/

# STEP watcher inbox (runtime drop folder; parts + results are transient)
/pipeline_in/
